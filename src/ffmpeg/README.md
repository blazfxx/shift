Place the FFmpeg browser build files in this directory:

- ffmpeg.js
- ffmpeg-util.js
- ffmpeg-core.js
- ffmpeg-core.wasm
- 814.ffmpeg.js

Shift loads these files locally at runtime so conversion stays on-device.
